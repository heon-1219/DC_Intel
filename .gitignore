__pycache__/
*.pyc
.venv/
*.egg-info/
.env
# Root-anchored: ignore the local SQLite scratch dir (./data/) WITHOUT swallowing nested source
# `data/` dirs like backend/app/auth/data/ (the common-passwords wordlist). (migration fix 2026-06-21)
/data/
backend/data/
node_modules/
frontend/dist/
ml/artifacts/*.pkl
backend/models/**/*.pkl
backend/dcintel.db
.pytest_cache/
.obsidian/
# editor/agent temp files (e.g. a *.md.tmp.PID.hash got tracked once); never commit these
*.tmp.*
